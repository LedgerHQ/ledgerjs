{
  "private": true,
  "name": "@ledgerhq/example-browser",
  "version": "1.0.4",
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@ledgerhq/hw-btc": "^1.0.4",
    "@ledgerhq/hw-comm-u2f": "^1.0.4",
    "@ledgerhq/hw-eth": "^1.0.4"
  },
  "devDependencies": {
    "flow-bin": "^0.63.1",
    "flow-typed": "^2.2.3"
  },
  "scripts": {
    "flow": "flow",
    "clean": "rm -rf lib/",
    "build": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --source-maps -d lib src",
    "watch": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --watch --source-maps -d lib src"
  }
}
