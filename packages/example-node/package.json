{
  "private": true,
  "name": "@ledgerhq/example-node",
  "version": "1.0.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@ledgerhq/eth": "^1.0.0",
    "@ledgerhq/btc": "^1.0.0",
    "@ledgerhq/comm-node-hid": "^1.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.59.0",
    "flow-typed": "^2.2.3"
  },
  "scripts": {
    "start": "node lib/index.js",
    "flow": "flow",
    "clean": "rm -rf lib/",
    "build":
      "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --source-maps -d lib src",
    "watch":
      "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --watch --source-maps -d lib src"
  }
}
