{
  "private": true,
  "name": "@ledgerhq/example-node",
  "version": "0.0.0",
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@ledgerhq/hw-app-btc": "^0.0.0",
    "@ledgerhq/hw-transport-node-hid": "^0.0.0",
    "@ledgerhq/hw-app-eth": "^0.0.0"
  },
  "devDependencies": {
    "flow-bin": "^0.63.1",
    "flow-typed": "^2.2.3"
  },
  "scripts": {
    "start": "node lib/index.js",
    "flow": "flow",
    "clean": "rm -rf lib/",
    "build": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --source-maps -d lib src",
    "watch": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --watch --source-maps -d lib src"
  }
}
