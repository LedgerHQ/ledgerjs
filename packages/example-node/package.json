{
  "private": true,
  "name": "@ledgerhq/example-node",
  "version": "1.0.2",
  "main": "index.js",
  "license": "Apache-2.0",
  "dependencies": {
    "@ledgerhq/hw-btc": "^1.0.2",
    "@ledgerhq/hw-comm-node-hid": "^1.0.2",
    "@ledgerhq/hw-eth": "^1.0.2"
  },
  "devDependencies": {
    "flow-bin": "^0.59.0",
    "flow-typed": "^2.2.3"
  },
  "scripts": {
    "start": "node lib/index.js",
    "flow": "flow",
    "clean": "rm -rf lib/",
    "build": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --source-maps -d lib src",
    "watch": "cd ../.. && export PATH=$(yarn bin):$PATH && cd - && babel --watch --source-maps -d lib src"
  }
}
